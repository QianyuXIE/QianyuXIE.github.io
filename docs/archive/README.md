# 归档文件

这些文件留作历史参考，不参与当前网站构建：

- `group1.json`：早期照片数据示例，图片地址是占位路径；Moment 现使用 `_moments/*.md`。
- `legacy-desk-home.css` 和 `legacy-desk-home.js`：旧版 2D 首页的样式和交互。
- `legacy-keynote.html`：未被页面引用的旧布局。

此目录在 `_config.yml` 中排除，不会作为网站页面发布。旧版 Travis 与 Codecov 配置已移除；当前发布由 GitHub Pages 完成。
